import { makeStyles } from "@material-ui/core";

const AvailabilitiesStyle = makeStyles(() => ({
  cardContainer: {
    display: "flex",
    flexDirection: "column",
    alignItems: "center"
  }
}));

export default AvailabilitiesStyle;
