import React from "react";

const Bookings = (props:any) => {
    const formatDate = `Y-m-d H:m:s`
    console.log("Bookings", props.location.state.date)
    console.log("Bookings2", props.location.state.doctorId)
  return <div></div>;
};

export default Bookings;
